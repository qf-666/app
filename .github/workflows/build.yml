name: build-and-release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  PROJECT_NAME: CryptoAlgorithmDemo
  SCHEME_NAME: CryptoAlgorithmDemo
  ARTIFACT_PREFIX: app-ios-unsigned

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Select an available iPhone simulator
        id: simulator
        shell: bash
        run: |
          DEVICE_NAME="$(xcrun simctl list devices available | grep -Eo 'iPhone[^()]+' | head -n 1 | xargs)"
          if [[ -z "$DEVICE_NAME" ]]; then
            echo "No available iPhone simulator was found." >&2
            xcrun simctl list devices available
            exit 1
          fi

          echo "device_name=$DEVICE_NAME" >> "$GITHUB_OUTPUT"
          echo "destination=platform=iOS Simulator,name=$DEVICE_NAME" >> "$GITHUB_OUTPUT"

      - name: Run unit tests
        shell: bash
        run: |
          xcodebuild \
            -project "$PROJECT_NAME.xcodeproj" \
            -scheme "$SCHEME_NAME" \
            -destination "${{ steps.simulator.outputs.destination }}" \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            test

      - name: Build unsigned release app for iPhoneOS
        shell: bash
        run: |
          xcodebuild \
            -project "$PROJECT_NAME.xcodeproj" \
            -scheme "$SCHEME_NAME" \
            -configuration Release \
            -sdk iphoneos \
            -destination "generic/platform=iOS" \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package unsigned IPA
        id: package
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            VERSION="${{ github.ref_name }}"
          else
            VERSION="snapshot-${GITHUB_SHA::7}"
          fi

          APP_PATH="$(find "$RUNNER_TEMP/DerivedData/Build/Products/Release-iphoneos" -maxdepth 1 -name '*.app' -print -quit)"
          if [[ -z "$APP_PATH" ]]; then
            echo "Unable to find the built .app bundle." >&2
            find "$RUNNER_TEMP/DerivedData/Build/Products" -maxdepth 2 -type d
            exit 1
          fi

          if [[ ! -f "$APP_PATH/Frameworks/GMObjC.framework/GMObjC" ]]; then
            echo "GMObjC.framework was not embedded into the app bundle." >&2
            find "$APP_PATH" -maxdepth 3 \( -name '*.framework' -o -name '*.app' \) -print
            exit 1
          fi

          ARTIFACT_NAME="${ARTIFACT_PREFIX}-${VERSION}"
          ARTIFACT_PATH="$RUNNER_TEMP/${ARTIFACT_NAME}.ipa"
          PACKAGE_DIR="$RUNNER_TEMP/${ARTIFACT_NAME}"

          rm -rf "$PACKAGE_DIR"
          mkdir -p "$PACKAGE_DIR/Payload"
          cp -R "$APP_PATH" "$PACKAGE_DIR/Payload/"
          (
            cd "$PACKAGE_DIR"
            zip -qry "$ARTIFACT_PATH" Payload
          )

          echo "artifact_name=$ARTIFACT_NAME" >> "$GITHUB_OUTPUT"
          echo "artifact_path=$ARTIFACT_PATH" >> "$GITHUB_OUTPUT"

      - name: Upload Actions artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.package.outputs.artifact_name }}
          path: ${{ steps.package.outputs.artifact_path }}
          if-no-files-found: error

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="${{ github.ref_name }}"
          TITLE="app iOS Demo ${TAG}"
          NOTES="Automated iOS Simulator build for ${TAG}."

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${{ steps.package.outputs.artifact_path }}" --clobber
          else
            gh release create "$TAG" "${{ steps.package.outputs.artifact_path }}" --title "$TITLE" --notes "$NOTES"
          fi
